{
  "name": "ptsd-webext",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build",
    "ext:dev": "npm run build && web-ext run -s dist --target=chromium --url https://www.twitch.tv/mynthos",
    "ext:build": "npm run build && web-ext build -s dist -a dist-webext -o"
  },
  "devDependencies": {
    "typescript": "^4.9.3",
    "vite": "^4.0.0",
    "web-ext": "^7.4.0"
  },
  "dependencies": {
    "@vue-reactivity/watch": "^0.2.0",
    "@vue/reactivity": "^3.2.45"
  }
}